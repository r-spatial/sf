# Summarize simple feature type for tibble

Summarize simple feature type / item for tibble

## Usage

``` r
type_sum.sfc(x, ...)

obj_sum.sfc(x)

pillar_shaft.sfc(x, ...)
```

## Arguments

- x:

  object of class `sfc`

- ...:

  ignored

## Details

see [type_sum](https://pillar.r-lib.org/reference/type_sum.html)
